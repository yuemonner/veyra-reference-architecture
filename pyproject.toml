[project]
name = "veyra-reference-architecture"
version = "0.1.0"
description = "Reference architecture for operational evidence reconstruction in Physical AI."
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
api = ["fastapi>=0.111", "uvicorn>=0.30"]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

